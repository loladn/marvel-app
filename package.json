{
  "name": "marvel-app",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server \"./src\" --files './src/**/*'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
